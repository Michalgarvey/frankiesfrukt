<% if signed_in? && current_user.admin? %>

  <h1>Add Products</h1>

  <%= render 'form', product: @product %>

  <%= link_to 'Back', products_path %>

<% else %>

<% link_to root_path %>


<% end %>
