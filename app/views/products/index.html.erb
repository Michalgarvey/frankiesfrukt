<p id="notice"><%= notice %></p>

<h1>Shop with Frankie</h1>
  <% if signed_in? && current_user.admin? %>
    <%= link_to 'Add New Product', new_product_path, class: 'btn btn-default pull-right'  %>
  <% end %>

<div class="container-fluid">
  <div class="row">
      <%= render @products %>
  </div>
</div>

<%= will_paginate @products %>
