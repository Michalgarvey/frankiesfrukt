<div class="col-md-12 order-list">
  <h4>
    Order # <%= @order.id %>:
    <%= link_to @order.product.name, @order.product %>
  </h4>
  <p><%= @order.product.description %></p>
  <strong>SEK <%= @order.product.price %>0</strong>
</div><!-- /col -->
